# Журнал сборки build-20260907-140427

## Состав

- Заведена: 2026-09-07T14:04:27Z
- База: 0598a13
- План: ai/builds/build-20260907-140427/plan.md

| подзадача | роль | исход | вне своих путей |
|---|---|---|---|
| search | run-task | принят | — |
| comments | run-task | принят | — |
| llm | run-task | принят (работа была незакоммичена — закоммитил дирижёр) | DECISIONS.md |
| pipeline | run-task | принят | DECISIONS.md (конфликт при слиянии разрешён: обе записи сохранены) |

## Уроки дирижёра

- Адаптер fraim неправильно парсит ответ Orca `worker-start`: берёт стадию
  (`dispatch_input`) как dispatch id; реальная форма id — `ctx_…`. Кэш `fleet.tsv`
  пришлось чинить вручную из `orca orchestration worker-list --json`. Кандидат в
  баг-репорт fraim (адаптер писан по справкам, живой прогон показал другую форму).
- Агент `devin` в Orca не принимает launch-time выбор модели — роль вида
  `devin:<model>:<effort>` в `ai/fraim.conf` валит весь `dispatch run`. Использовать
  агентов с поддержкой `--model` (claude/codex/cursor) или патчить fraim.
- Воркеры-агенты считают `DECISIONS.md` «своим» файлом, даже когда он не объявлен в
  плане: два из четырёх тронули его. В будущих планах либо явно объявлять
  фундаментные файлы в одной выделенной подзадаче, либо жёстче формулировать запрет.
- Работа воркера может остаться незакоммиченной в его чекауте при статусе
  `succeeded` — сверка по дереву должна смотреть и `git status`, а не только
  `git diff base..HEAD`.
